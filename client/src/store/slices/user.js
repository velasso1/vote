import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const user = createSlice({
  name: "user",
  initialState,
  reducers: {},
});

// Actions

const login = () => {
  return (dispatch) => {
    fetch();
  };
};
